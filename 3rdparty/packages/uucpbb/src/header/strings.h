#define _STRINGS.H_

extern char
      *strcat(),
      *strncat(),
      *strhcpy(),
      *strcpy(),
      *strncpy(),
      *strclr(),
      *strucat(),
      *strucpy(),
      *index(),
      *rindex(),
      *reverse(),
      *strend(),
      *memset(),
      *memchr(),
      *memcpy(),
      *strspn(),
      *strcspn(),
      *strtok(),
      *strpbrk();

extern int
      strcmp(),
      strncmp(),
      strlen(),
      strucmp(),
      strnucmp(),
      patmatch(),
      memcmp();
