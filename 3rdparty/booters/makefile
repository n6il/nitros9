include ../../Makefile.rules

DEPENDS		= ./Makefile
BOOTERS		= boot_kenton boot_rampak boot_ide boot_tc3 boot_wd1002 \
		boot_rom
ALLOBJS		= $(BOOTERS)

all:	$(ALLOBJS) $(DEPENDS)

clean:
	$(RM) $(ALLOBJS)

